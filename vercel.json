{
  "version": 2,
  "framework": null,
  "buildCommand": "python -m pip install --upgrade pip && python -m pip install mkdocs-material && python -m pip install -r requirements.txt && python -m mkdocs build",
  "outputDirectory": "site",
  "installCommand": "python -m pip install -r requirements.txt"
}